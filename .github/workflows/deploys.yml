name: Deploy to Zeit

on:
  push:
    branches:
      - dev
      - feature/*
      - master
      - release/*

jobs:
  deploy:
    name: Deploy to Zeit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Now
        if: github.ref == 'refs/heads/dev'
        run:
          NOW_ORG_ID=team_WtAI5aOHuDxYRprOgqAOqdAS NOW_PROJECT_ID=QmXGZT7Mta1bgttLGhBLdzShPyWSauKi7f2x3DtN1A17ma now -A now.river.dev.json -t ${{ secrets.ZEIT_TOKEN }}
